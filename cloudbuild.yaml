options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/hale-wind-461813-i3/duel', '-f', 'duel/duel/Dockerfile', 'duel/duel']

images:
  - 'gcr.io/hale-wind-461813-i3/duel'
